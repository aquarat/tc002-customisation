*.pkg
*.dmg
.DS_Store
/tokens
panel-v2/mock-tokens
__pycache__/
# vendor firmware images and partition dumps are ulanzi's, not ours
*.img
*.sqsh
*.bin
# local-only artefact store: the vendor image, our built image, the partition
# dumps and the static busybox, with provenance and checksums. see
# firmware-local/PROVENANCE.md
/firmware-local/
